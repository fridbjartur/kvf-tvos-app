/** MiKS — the TV youth front page (`sjon/miks`). */

import { SectionScreen } from "@/components/section-screen";

export default function MiksScreen() {
  return <SectionScreen section="sjon-miks" />;
}
