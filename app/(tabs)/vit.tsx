/** VIT — the TV children's front page (`sjon/vit`). */

import { SectionScreen } from "@/components/section-screen";

export default function VitScreen() {
  return <SectionScreen section="sjon-vit" />;
}
